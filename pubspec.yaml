name: scenebox
publish_to: "none"
description: SceneBox Android Flutter port
version: 1.0.0+1
environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.5.0

flutter:
  uses-material-design: true
